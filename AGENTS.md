# Agent instructions

## Agent skills

### Issue tracker

GitHub Issues on this repository; use the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles mapped to GitHub label strings (defaults match role names). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: optional `CONTEXT.md` and `docs/adr/` at repo root when they exist. See `docs/agents/domain.md`.
